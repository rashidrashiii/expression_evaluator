name: expression_evaluator
description: A Dart library to evaluate dynamic expressions with logical operators.
version: 1.0.6
repository: https://github.com/rashidrashiii/expression_evaluator.git

environment:
  sdk: ">=2.17.0 <4.0.0"

# Add regular dependencies here.
dependencies:
  # path: ^1.8.0

dev_dependencies:
  lints: ^2.1.0
  test: ^1.24.0
